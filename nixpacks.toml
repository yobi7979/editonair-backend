[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "gcc", "zlib", "libjpeg", "libpng", "freetype"]

[phases.install]
cmds = ["pip install --only-binary=:all: -r requirements.txt"]

[start]
cmd = "python app.py" 